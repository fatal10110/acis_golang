## Educational Purpose Only

This project is created **exclusively for educational purposes**. It demonstrates the capability of AI systems to port large, complex projects from one programming language (Java) to another (Go).

## Project Overview

The entire codebase was generated by AI as part of a learning exercise to evaluate:

- Large-scale software architecture translation between languages
- Maintenance of business logic across language boundaries
- AI's ability to handle complex, domain-specific legacy codebases
- Automated refactoring and code generation at enterprise scale

## Code Generation

**All code in this repository was generated by AI.** No manual implementation was performed. This includes:

- Game server logic and packet handling
- Network protocols and data structures
- Login server functionality
- Game data loaders and parsers
- Database access layers
- Game mechanics and effect systems

## Legal Notice

This is **not** an official project. It is a standalone educational demonstration. Use of this code is governed by applicable laws in your jurisdiction. The creator assumes no liability for misuse or violations of intellectual property rights.

## Disclaimer

This codebase is provided as-is for educational and research purposes. Users are solely responsible for ensuring their use complies with all applicable laws and regulations.

## Oracle diff tooling

`cmd/datadiff` writes stable, field-level dumps for the M3 loader categories currently wired into the Go port and can compare them against an expected dump:

```bash
go run ./cmd/datadiff -list
go run ./cmd/datadiff -category=skill > skill.dump
go run ./cmd/datadiff -category=skill -expected-dump=skill.dump
```

`-datapack` is optional when `aCis_datapack/` sits next to the checkout or `ACIS_DATAPACK` is set. Current category coverage is the output of `-list`.
