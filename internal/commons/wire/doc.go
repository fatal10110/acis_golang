// Package wire encodes and decodes the little-endian primitives shared by
// every wire protocol in this server: game packets, login packets, and the
// game-server/login-server link protocol.
package wire
